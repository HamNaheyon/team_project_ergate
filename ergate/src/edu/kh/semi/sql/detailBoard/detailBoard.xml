<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>

	<!-- 프리랜서 게시글 상세 조회 -->
	<entry key="freeSelectBoard">
		SELECT * FROM FREE_BOARD_DETAIL
		WHERE BOARD_NO= ?
	</entry>
	
	<!-- 기업 게시글 상세 조회 -->
	<entry key="ComSelectBoard">
		SELECT * FROM COM_BOARD_DETAIL 
		WHERE BOARD_NO= ?
	</entry>
	
	<!-- 게시글 조회 수 증가 -->
	<entry key="increaseReadCount">
		UPDATE BOARD SET 
		READ_COUNT = READ_COUNT + 1
		WHERE BOARD_NO = ?
	</entry>
</properties>